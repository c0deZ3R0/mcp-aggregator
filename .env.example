# FastMCP Environment Variables
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control!

# ============================================
# MCP Server Authentication
# ============================================

# API token for securing the /mcp endpoint
# Use this to authenticate MCP clients connecting to FastMCP
# Leave empty to disable authentication
MCP_API_TOKEN=your-secret-mcp-token-here

# ============================================
# Web UI Authentication
# ============================================

# Password for the web dashboard at http://localhost:3050
# Default: admin
UI_PASSWORD=your-secure-password-here

# ============================================
# Upstream Server Credentials
# ============================================

# Example: API key for GoFastMCP or other HTTP-based servers
# Reference this in server configs using $GOFASTMCP_API_KEY
#GOFASTMCP_API_KEY=your-gofastmcp-api-key

# Example: Bearer token for authenticated HTTP servers
# Reference this in server configs using $HTTP_BEARER_TOKEN
#HTTP_BEARER_TOKEN=your-http-bearer-token

# Example: API key for external services
# Reference this in server configs using $EXTERNAL_API_KEY
#EXTERNAL_API_KEY=your-external-api-key

# ============================================
# Service Configuration
# ============================================

# Optional: Custom port for Serena service (default: 9121)
# SERENA_PORT=9121

# Optional: Custom timeout for service startup in seconds (default: 30)
# SERVICE_STARTUP_TIMEOUT=30

# ============================================
# Notes
# ============================================
# - To use environment variables in server configs, prefix with $ (e.g., $MY_API_KEY)
# - All sensitive values should be stored in .env, not in code
# - .env is gitignored and will not be committed to version control
# - For production, use a secrets management system instead of .env files
